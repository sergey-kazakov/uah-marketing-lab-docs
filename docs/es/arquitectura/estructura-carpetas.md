# Estructura de carpetas

La estructura propuesta del monorepo es:

- `apps/`: aplicaciones individuales (Posicionamiento, SegMentor, etc.)
- `packages/`: paquetes compartidos (UI kit, clustering, generación de datasets)
- `docs/`: documentación técnica (este manual)
