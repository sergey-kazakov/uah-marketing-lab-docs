# Folder structure

The proposed monorepo structure is:

- `apps/`: individual applications (Posicionamiento, SegMentor, etc.)
- `packages/`: shared packages (UI kit, clustering, dataset generators)
- `docs/`: technical documentation (this manual)
